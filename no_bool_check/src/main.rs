fn main() {
    println!("Hello World!");
    let x = 5;
    if x == 5 {
        // Rust would always need a boolean check. so we can't write something like "if x {}"
        println!("x is equal to 5.")
    }
}
